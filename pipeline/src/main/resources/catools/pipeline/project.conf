catools.pipeline {
  project {
    // Pipeline Project Code (Max length = 5 char)
    code = ""
    code = ${?CATOOLS_PROJECT_CODE}
    code = ${?CATOOLS_PIPELINE_PROJECT_CODE}

    // Pipeline Project Name (Max length = 50 char)
    name = ""
    name = ${?CATOOLS_PROJECT_NAME}
    name = ${?CATOOLS_PIPELINE_PROJECT_NAME}
  }
}
