catools.pipeline {
  environment {
    // Pipeline Environment Code
    code = ""
    code = ${?CATOOLS_ENVIRONMENT_CODE}
    code = ${?CATOOLS_PIPELINE_ENVIRONMENT_CODE}

    // Pipeline Environment Name
    name = ""
    name = ${?CATOOLS_ENVIRONMENT_NAME}
    name = ${?CATOOLS_PIPELINE_ENVIRONMENT_NAME}
  }
}
